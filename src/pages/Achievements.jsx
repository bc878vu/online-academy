import { useEffect, useState } from "react";
import { auth } from "../firebase";
import { getGamificationProfile } from "../services/gamificationService";
const BADGES = [{ id: "first_step", icon: "🌱", title: "First Step", text: "Complete your first rewarded activity." }, { id: "streak_7", icon: "🔥", title: "7 Day Streak", text: "Learn for seven consecutive days." }, { id: "quiz_master", icon: "🧠", title: "Quiz Master", text: "Build a strong quiz record." }, { id: "course_master", icon: "👑", title: "Course Master", text: "Complete a full learning journey." }];
export default function Achievements() { const [items, setItems] = useState([]); useEffect(() => { if (auth.currentUser?.uid) getGamificationProfile(auth.currentUser.uid).then((p) => setItems(p.achievements || [])); }, []); const unlocked = new Set(items.map((x) => x.id)); return <main className="min-h-screen bg-slate-50 px-4 py-8"><div className="mx-auto max-w-5xl"><h1 className="text-3xl font-black text-slate-900">Achievements</h1><p className="mt-2 text-slate-500">Keep learning to unlock your collection.</p><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{BADGES.map((b) => <article key={b.id} className={`rounded-2xl border p-5 ${unlocked.has(b.id) ? "border-blue-200 bg-white shadow-sm" : "border-slate-200 bg-slate-100 opacity-60 grayscale"}`}><div className="text-4xl">{b.icon}</div><h2 className="mt-4 font-black">{b.title}</h2><p className="mt-2 text-sm text-slate-500">{b.text}</p><p className="mt-4 text-xs font-black uppercase">{unlocked.has(b.id) ? "Unlocked" : "Locked"}</p></article>)}</div></div></main>; }
