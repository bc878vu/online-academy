import { useState } from "react";
import { auth } from "../firebase";
import { rewardLearningEvent } from "../services/gamificationService";
const Q = [{ q:"HTML is mainly used for?", a:["Structure","Styling","Database","Hosting"], c:0 },{ q:"Which keyword creates a constant?", a:["var","let","const","static"], c:2 },{ q:"CSS is mainly used for?", a:["Logic","Styling","Storage","Routing"], c:1 }];
export default function LearningGames(){const[i,setI]=useState(0),[score,setScore]=useState(0),[done,setDone]=useState(false);const answer=async n=>{const correct=n===Q[i].c,next=score+(correct?1:0);setScore(next);if(i===Q.length-1){setDone(true);if(auth.currentUser) await rewardLearningEvent(auth.currentUser.uid,{eventId:`speed-game-${new Date().toISOString().slice(0,10)}`,xp:next*10,coins:next*2,type:"speed-quiz",meta:{score:next,total:Q.length}});}else setI(i+1)};if(done)return <main className="min-h-screen bg-slate-50 p-6"><div className="mx-auto max-w-xl rounded-3xl bg-white p-8 text-center shadow"><div className="text-6xl">🏆</div><h1 className="mt-4 text-3xl font-black">Challenge Complete</h1><p className="mt-3 text-slate-500">Score: {score}/{Q.length}. Your XP reward is protected from duplicate claims.</p><button onClick={()=>{setI(0);setScore(0);setDone(false)}} className="mt-6 rounded-xl bg-blue-600 px-5 py-3 font-bold text-white">Play Again</button></div></main>;return <main className="min-h-screen bg-slate-50 p-6"><div className="mx-auto max-w-xl rounded-3xl bg-white p-6 shadow"><p className="text-sm font-black text-blue-600">⚡ SPEED QUIZ · {i+1}/{Q.length}</p><h1 className="mt-4 text-2xl font-black">{Q[i].q}</h1><div className="mt-6 space-y-3">{Q[i].a.map((x,n)=><button key={x} onClick={()=>answer(n)} className="w-full rounded-xl border border-slate-200 p-4 text-left font-bold hover:border-blue-400 hover:bg-blue-50">{x}</button>)}</div></div></main>}
